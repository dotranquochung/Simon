library ieee;
use ieee.std.logic.1164.all;
use ieee.numeric_std.all;

ENTITY VGA is
PORT(
  CLOCK_25: in STD_LOGIC_VECTOR (1 downto 0);
  VGA_HS; VGA_VS: out STD_LOGIC;
  VGA_R, VGA_G, VGA_B: out STD_LOGIC_VECTOR (3 downto 0));
END VGA;


